# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
frontend/.next/
frontend/out/
frontend/next-env.d.ts
frontend/tsconfig.tsbuildinfo

# Environment secrets
.env
.env.local
.env*.local
!.env.example
!frontend/.env.example

# Compact compile output (large binary artifacts — regenerate via `compact compile`)
build/

# OS/editor cruft
.DS_Store
*.log
.vscode/

# Files the Claude desktop app drops into this folder when it delivers
# generated files during a chat session -- not meant to be versioned.
Claude outputs/

# Claude Code project config and skills installed via `npx skills add`
# -- local tooling, not part of the product.
.claude/
skills-lock.json

# v2 planning and working docs (CLAUDE.md, docs/v2/). Kept local on purpose;
# .claude/plan/ is already covered by .claude/ above.
CLAUDE.md
docs/v2/
